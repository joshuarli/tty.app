# tty.app

standard issue terminal emulator

- only supports apple silicon
- weighs less than 200 kb
- xterm-256color subset sufficient for tmux
- damage-tracking
- renders every frame in a single Metal compute shader dispatch


## install

edit `src/config.rs` and run `make install`

`brew install --cask font-hack`

